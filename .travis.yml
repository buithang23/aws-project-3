language: node_js
node_js:
  - 13

services:
  - docker

# Pre-testing installs
install:
  - echo "nothing needs to be installed"

# Scripts to be run such as tests
before_script:
  - echo "no tests"

script:
  - docker --version # print the version for logging
  - docker build -t reverseproxy
  - docker build -t udagram-api-user
  - docker build -t udagram-api-feed
  - docker build -t udagram-frontend
  # tag
  - docker tag reverseproxy buithang23/reverseproxy:latest
  - docker tag udagram-api-user buithang23/udagram-api-user:latest
  - docker tag udagram-api-feed buithang23/udagram-api-feed:latest
  - docker tag udagram-frontend buithang23/udagram-frontend:latest
  

# Tasks to perform after the process is successful. Formatting the Docker username and password as below enables you to programmatically log in without having the password exposed in logs.
after_success:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - docker push buithang23/reverseproxy
  - docker push buithang23/udagram-api-user
  - docker push buithang23/udagram-api-feed
  - docker push buithang23/udagram-frontend